# ICSPA 项目总结

## 项目概述

本项目是 MOOC 课程《计算机系统基础（五）：x86 模拟器编程实践》的代码仓库，是一个完整的 x86 架构模拟器实现项目。该项目旨在帮助学生深入理解计算机系统的底层运作原理，通过编写和完善一个功能完整的 x86 模拟器来学习计算机组成原理、操作系统和系统编程。

### 课程资源

- **实验手册地址**：
  - GitHub: http://github.com/ics-nju-wl/icspa-public-guide
  - Gitee: https://gitee.com/wlicsnju/icspa-public-guide

- **代码仓库地址**：
  - GitHub: http://github.com/ics-nju-wl/icspa-public
  - Gitee: https://gitee.com/wlicsnju/icspa-public

## 项目架构

项目采用模块化设计，主要包含以下四个核心组件：

### 1. NEMU (NJU Emulator) - x86 模拟器核心 (`nemu/`)

NEMU 是整个项目的核心，实现了一个完整的 x86 架构模拟器。

#### 主要模块：

- **CPU 模拟** (`nemu/src/cpu/`)
  - **寄存器实现** (`reg.c`): 实现了 x86 的通用寄存器、段寄存器、控制寄存器等
  - **ALU 实现** (`alu.c`): 算术逻辑单元，包含加减乘除、位运算等基本操作
  - **FPU 实现** (`fpu.c`): 浮点运算单元，支持 x87 浮点指令集
  - **指令解码** (`decode/`): 指令解码器，负责解析 x86 机器码
  - **指令执行** (`instr/`): 包含 55+ 个 x86 指令的实现，包括：
    - 数据传送指令：`mov`, `push`, `pop`, `lea` 等
    - 算术运算指令：`add`, `sub`, `mul`, `div`, `imul`, `idiv` 等
    - 逻辑运算指令：`and`, `or`, `xor`, `not`, `test` 等
    - 移位指令：`shl`, `shr`, `sal`, `sar` 等
    - 控制转移指令：`jmp`, `call`, `ret`, `jcc` (条件跳转) 等
    - 字符串操作指令：`movs`, `cmps`, `stos` 等
    - 系统指令：`int`, `iret`, `hlt`, `cli`, `sti` 等
    - 浮点指令：`x87.c` 实现的浮点运算指令
  - **中断处理** (`intr.c`): 中断和异常处理机制

- **内存管理** (`nemu/src/memory/`)
  - **物理内存** (`memory.c`): 物理内存模拟
  - **MMU** (`mmu/`): 内存管理单元，支持分段和分页机制

- **设备模拟** (`nemu/src/device/`)
  - **8259 PIC** (`i8259_pic.c`): 可编程中断控制器
  - **SDL 支持** (`sdl.c`): 图形和音频设备支持
  - **I/O 设备** (`io/`, `dev/`): 各种输入输出设备模拟

- **监控器** (`nemu/src/monitor/`)
  - 调试监控器，提供断点、单步执行等调试功能

#### 源代码统计：
- C 源文件：91 个
- 指令实现：55 个独立的指令文件
- 总代码行数：约 1,561 行（仅指令实现部分）

### 2. Kernel - 简易操作系统内核 (`kernel/`)

实现了一个简化的操作系统内核，运行在 NEMU 之上。

#### 主要组件：

- **启动代码** (`start/start.S`): 系统启动的汇编代码
- **主程序** (`main.c`): 内核初始化和主循环
- **内存管理** (`memory/`): 虚拟内存管理、分页机制
- **文件系统** (`fs/`): 简单的文件系统实现
- **设备驱动** (`driver/`): 各种设备的驱动程序
- **中断处理** (`irq/`): 中断和异常处理，包含 `do_irq.S`
- **系统调用** (`syscall/`): 系统调用接口
- **ELF 加载器** (`elf/`): 可执行文件加载
- **库函数** (`lib/`): 内核使用的库函数

#### 源代码统计：
- C 源文件：17 个
- 支持的功能：分页、中断、系统调用、文件系统、设备驱动

### 3. Testcase - 测试用例集 (`testcase/`)

包含 32 个 C 语言和汇编语言编写的测试程序，用于验证模拟器的正确性。

#### 测试用例类型：

- **基础测试**：
  - `mov.S`, `mov-cmp.S`: 数据传送和比较指令测试
  - `mov-c.c`, `mov-jcc.c`: C 语言编写的移动和跳转测试
  
- **算术运算测试**：
  - `add.c`, `add-longlong.c`: 加法和长整数加法
  - `sub-longlong.c`: 长整数减法
  - `mul-longlong.c`: 长整数乘法
  
- **排序算法测试**：
  - `bubble-sort.c`: 冒泡排序
  - `quick-sort.c`: 快速排序
  - `select-sort.c`: 选择排序
  
- **数学计算测试**：
  - `prime.c`: 素数计算
  - `fact.c`: 阶乘计算
  - `fib.c`: 斐波那契数列
  - `pascal.c`: 帕斯卡三角形
  - `matrix-mul.c`, `matrix-mul-small.c`: 矩阵乘法
  
- **逻辑和控制流测试**：
  - `if-else.c`: 条件分支
  - `leap-year.c`: 闰年判断
  - `max.c`, `min3.c`: 最大最小值
  
- **字符串和 I/O 测试**：
  - `hello-str.c`: 字符串输出
  - `hello-inline.c`: 内联汇编测试
  - `echo.c`: 回显程序
  - `string.c`: 字符串操作
  
- **特殊测试**：
  - `test-float.c`: 浮点运算测试
  - `struct.c`: 结构体测试
  - `bit.c`: 位操作测试
  - `movsx.c`: 符号扩展测试
  - `32_keyboard_inline.c`: 键盘输入测试

### 4. Game - 游戏应用 (`game/`)

包含在模拟器上运行的游戏应用，展示系统的完整功能。

#### 主要游戏：

- **NEMU-PAL** (`nemu-pal/`): 
  - SDL-based 的《仙剑奇侠传》(Legend of Sword and Fairy) 移植版本
  - 这是一个经典的中文 RPG 游戏
  - 完整的游戏引擎实现，包含 12 个子目录
  - 支持图形、音频、游戏逻辑等完整功能
  
- **Typing Game** (`typing/`): 打字游戏
- **Credits** (`credits/`): 制作人员名单展示

#### 源代码统计：
- C 源文件：46 个
- 主要基于 SDL 库实现图形和交互

### 5. 库文件 (`libs/`)

- **newlib**: 标准 C 库的移植
- **nemu-ref**: NEMU 的参考实现

### 6. 构建和脚本 (`scripts/`)

包含构建、测试和评分相关的脚本文件。

## 项目特点

### 1. 完整的 x86 指令集支持
- 实现了 55+ 个常用 x86 指令
- 支持整数运算、浮点运算、位操作、控制转移等
- 包含完整的 x87 FPU 指令支持

### 2. 分阶段的实验设计
项目分为 4 个 PA (Programming Assignment) 阶段：

- **PA-1**: 基础部分
  - 实现寄存器和 ALU
  - 测试基本算术和逻辑运算
  - 测试 FPU 浮点运算

- **PA-2**: 指令执行
  - **PA-2-1**: 基本指令实现（无内核模式）
  - **PA-2-2**: 在内核模式下运行测试
  - **PA-2-3**: 表达式求值测试

- **PA-3**: 内存和缓存
  - **PA-3-1**: 内存管理基础
  - **PA-3-2**: 虚拟内存
  - **PA-3-3**: 完整的内存系统

- **PA-4**: 操作系统和应用
  - **PA-4-1**: 系统调用和内核功能
  - **PA-4-2**: 完整的系统调用支持
  - **PA-4-3**: 运行游戏（最终目标）

### 3. 完善的测试体系
- 32 个独立的测试用例
- 覆盖从简单到复杂的各种场景
- 支持自动化测试和批量测试
- 包含针对每个阶段的专门测试目标

### 4. 真实应用支持
- 可以运行完整的操作系统内核
- 支持系统调用、中断、设备驱动
- 能够运行复杂的游戏应用（仙剑奇侠传）

## 构建和使用

### 基本命令

```bash
# 编译 NEMU 模拟器、测试用例和内核
make nemu

# 运行简单测试（无内核）
make run

# 运行内核模式测试
make run-kernel

# 调试模式
make debug

# 编译游戏
make game

# 清理构建文件
make clean
```

### 测试命令

```bash
# PA-1 测试：寄存器、ALU、FPU
make test_pa-1

# PA-2-1 测试：基本指令（无内核）
make test_pa-2-1

# PA-2-2 测试：内核模式基本测试
make test_pa-2-2

# PA-2-3 测试：表达式求值
make test_pa-2-3

# PA-3-1/3-2 测试：内存管理
make test_pa-3-1
make test_pa-3-2

# PA-3-3 测试：完整内存系统
make test_pa-3-3

# PA-4-1/4-2 测试：系统调用
make test_pa-4-1
make test_pa-4-2

# PA-4-3 测试：运行游戏（最终测试）
make test_pa-4-3
```

### 运行单个测试用例

```bash
# 运行简单的加法测试
./nemu/nemu --testcase add

# 在内核模式下运行测试
./nemu/nemu --kernel --testcase add

# 自动运行模式
./nemu/nemu --autorun --testcase bubble-sort

# 运行游戏
./nemu/nemu --test-game [STU_ID] --kernel --autorun
```

### 调试选项

```bash
# 测试寄存器
./nemu/nemu --test-reg

# 测试特定 ALU 操作
./nemu/nemu --test-alu add

# 测试 FPU 操作
./nemu/nemu --test-fpu mul
```

## 技术栈

- **编程语言**: C 语言、x86 汇编
- **构建工具**: GNU Make
- **调试工具**: GDB
- **图形库**: SDL (Simple DirectMedia Layer)
- **标准库**: newlib
- **体系结构**: x86 (IA-32)

## 项目规模

### 代码统计
- **总 C 源文件**: 186 个
- **总头文件**: 233 个
- **主要组件代码量**:
  - NEMU 模拟器：91 个 C 文件
  - 操作系统内核：17 个 C 文件
  - 游戏应用：46 个 C 文件
  - 测试用例：32 个 C/汇编文件

### 功能统计
- x86 指令实现：55+ 个
- 测试用例：32 个
- 实验阶段：4 个主要阶段（PA-1 到 PA-4）
- 子阶段：12 个详细阶段（包含各种测试目标）

## 学习目标

通过完成本项目，学生将能够：

1. **深入理解计算机体系结构**
   - x86 指令集架构
   - CPU 的工作原理（取指、译码、执行、访存、写回）
   - 寄存器、ALU、FPU 的实现

2. **掌握系统级编程**
   - C 语言系统编程
   - 汇编语言编程
   - 内存管理和指针操作

3. **理解操作系统原理**
   - 中断和异常处理
   - 虚拟内存和分页机制
   - 系统调用实现
   - 设备驱动开发

4. **培养调试能力**
   - 使用 GDB 调试系统级代码
   - 理解程序执行的底层细节
   - 问题诊断和解决能力

5. **完整的系统开发经验**
   - 从底层硬件到高层应用的完整视野
   - 模块化设计和分层实现
   - 大型项目的组织和管理

## 项目亮点

1. **教学价值高**: 通过实际编写模拟器，学生能够真正理解计算机系统的工作原理
2. **循序渐进**: 分阶段的设计使学习曲线平滑，便于逐步深入
3. **实用性强**: 最终能够运行真实的游戏，增强成就感和学习动力
4. **完整性**: 涵盖了从 CPU、内存到操作系统、应用程序的完整系统栈
5. **可扩展**: 模块化设计使得项目易于扩展和改进

## 许可证

本项目用于教学目的。

游戏部分（NEMU-PAL）使用 GNU General Public License v3 (GPLv3) 授权，原始游戏数据文件版权归 SoftStar Inc. 所有，本项目不包含任何原始游戏的代码或数据文件。

---

**注**: 本总结基于对项目代码的分析，详细的实验指导请参考配套的实验手册。
